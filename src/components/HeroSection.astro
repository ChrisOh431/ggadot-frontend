---
interface Props {
  backgroundOverride?: boolean;
}

const { backgroundOverride } = Astro.props;
---

<div
  class=`hero ${backgroundOverride ? "" : "bg-ggviolet-primary"} py-32 lg:py-40 w-full text-center w-full h-full flex flex-col items-center justify-center text-white font-extralight gap-3 px-6 lg:px-8`>
  <slot />
</div>
